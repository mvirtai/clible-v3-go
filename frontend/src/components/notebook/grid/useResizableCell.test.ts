// src/components/notebook/useResizableCell.test.ts
import { describe, it, expect } from 'vitest';

describe('useResizableCell.ts test suite', () => {
  it('redirects to useResizableCell.test.tsx for full React hook testing', () => {
    expect(true).toBe(true);
  });
});
